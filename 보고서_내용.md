<div align="center">
<br><br><br><br><br><br>

# 시스템 프로그래밍 프로젝트 #2 보고서

<br>


**인공지능공학부 20234329 안순광**

<br><br><br><br><br><br><br><br><br>
</div>

---


## 목차

- [시스템 프로그래밍 프로젝트 #2 보고서](#시스템-프로그래밍-프로젝트-2-보고서)
  - [목차](#목차)
  - [1. 설계/구현 아이디어](#1-설계구현-아이디어)
    - [1.1 전체 구조](#11-전체-구조)
    - [1.2 자료구조 설계](#12-자료구조-설계)
    - [1.3 명령어 테이블 로드](#13-명령어-테이블-로드)
    - [1.4 토큰화 구현](#14-토큰화-구현)
    - [1.5 PASS 1 구현](#15-pass-1-구현)
    - [1.6 객체코드 생성](#16-객체코드-생성)
    - [1.7 출력 형식](#17-출력-형식)
  - [2. 수행결과](#2-수행결과)
    - [2.1 테스트 환경](#21-테스트-환경)
    - [2.2 실행 결과](#22-실행-결과)
  - [3. 결론 및 보충할 점](#3-결론-및-보충할-점)
    - [3.1 성과](#31-성과)
    - [3.2 보충할 점](#32-보충할-점)
  - [4. 소스코드](#4-소스코드)
  - [5. 깃허브 URL](#5-깃허브-url)

---

## 1. 설계/구현 아이디어

### 1.1 전체 구조
본 프로젝트는 SIC 어셈블러 개발의 사전 단계로, 입력 프로그램을 토큰 단위로 파싱하고 각 명령어에 해당하는 OPCODE와 OPERAND를 매핑하여 기계어 코드를 생성하는 프로그램을 구현했다. 프로그램은 크게 명령어 테이블 관리, 입력 파일 처리, 토큰화, 심볼 테이블 생성, 객체코드 생성의 5단계로 구성했다.

### 1.2 자료구조 설계
과제 명세에서 제시된 구조체를 기반으로 설계했다. `inst_unit` 구조체는 명령어의 mnemonic, opcode, format, 피연산자 개수를 저장하며, `token_unit` 구조체는 각 라인을 label, operator, operand, comment로 분리하여 관리한다. 추가로 `sym_unit` 구조체를 통해 심볼 테이블을 구현하여 라벨과 주소를 매핑했다.

### 1.3 명령어 테이블 로드
`load_inst_table` 함수는 inst.data.txt 파일을 읽어 명령어 정보를 파싱했다. 파일은 탭으로 구분된 형식(MNEMONIC, TYPE, FORMAT, OPCODE)으로 구성되어 있으며, sscanf를 사용해 각 필드를 추출했다. OPCODE는 16진수 문자열로 저장되어 있어 sscanf의 %X 형식 지정자를 사용해 파싱했다. 총 70개의 SIC/XE 명령어를 inst_table 배열에 저장했다.

### 1.4 토큰화 구현
`tokenize_line` 함수는 입력 라인을 파싱하여 token 구조체를 생성한다. SIC 어셈블리 형식의 특징인 "라벨은 첫 칼럼부터 시작"이라는 규칙을 적용했다. 라인의 첫 문자가 공백이나 탭이 아니면 라벨이 있는 것으로 판단하고, strtok 함수로 공백/탭 기준으로 토큰을 분리했다. 피연산자는 콤마로 구분될 수 있어 추가로 콤마 기준 파싱을 수행했다. 주석 라인('.'로 시작)은 is_comment_line 플래그로 표시하여 출력에서 제외했다.

### 1.5 PASS 1 구현
`pass1_build_symtab` 함수는 Location Counter(LOCCTR)를 관리하며 각 라인의 주소를 계산했다. START 지시자에서 시작 주소를 파싱하고, 각 명령어와 지시자의 크기에 따라 LOCCTR을 증가시켰다. Format 3 명령어는 3바이트, WORD는 3바이트, RESW는 피연산자×3바이트, RESB는 피연산자 바이트만큼 증가시켰다. BYTE 지시자는 C'문자열' 형식은 문자열 길이만큼, X'16진수' 형식은 16진수 길이의 절반만큼 증가시켰다. 라벨이 있는 라인은 현재 LOCCTR 값을 심볼 테이블에 저장했다.

### 1.6 객체코드 생성
`generate_object_code` 함수는 각 토큰에 대해 기계어 코드를 생성했다. Format 3 명령어는 OPCODE(8비트) + 주소(16비트)로 구성되며, 피연산자가 심볼인 경우 심볼 테이블에서 주소를 찾아 삽입했다. 인덱싱 모드(예: BUFFER,X)는 비트 15를 설정하여 처리했다. WORD 지시자는 피연산자 값을 6자리 16진수로 변환했다. BYTE C'문자열'은 각 문자의 ASCII 코드를 16진수로 변환하고, BYTE X'16진수'는 따옴표 안의 값을 그대로 사용했다. RESW와 RESB는 메모리만 예약하므로 객체코드를 생성하지 않았다.

### 1.7 출력 형식
`print_object_listing` 함수는 교재의 Fig 2.2 형식으로 결과를 출력했다. 각 라인마다 주소(4자리 16진수), 라벨, 연산자, 피연산자, 객체코드 순서로 탭으로 구분하여 출력했다. 주석 라인과 operator가 없는 빈 라인은 출력에서 제외했다.

## 2. 수행결과

### 2.1 테스트 환경
- 컴파일러: gcc (MinGW-w64)
- 컴파일 옵션: -std=c11 -O2 -Wall -Wextra
- 운영체제: Windows
- 입력 파일: 교재의 Fig 2.1(교재의 input) 참조

### 2.2 실행 결과
프로그램을 `a.exe inst.data.txt test_input.txt` 명령으로 실행했다. 총 51줄의 소스코드를 처리하여 주석 라인을 제외한 48줄의 결과를 출력했다. 주요 결과는 다음과 같다.

- START 지시자: 시작 주소 0x1000 설정
- 명령어 처리: STL, JSUB, LDA 등 모든 Format 3 명령어의 객체코드 정상 생성
- 심볼 참조: RETADR(0x1033), LENGTH(0x1036), BUFFER(0x1039) 등 모든 심볼의 주소가 정확히 계산됨
- 인덱싱 모드: STCH BUFFER,X와 LDCH BUFFER,X 명령어에서 비트 15 설정 확인 (0x549039, 0x509039)
- 지시자 처리:
  - BYTE C'EOF': 454F46 (각 문자의 ASCII 코드)
  - BYTE X'F1': F1
  - BYTE X'05': 05
  - WORD 3: 000003
  - WORD 4096: 001000
  - RESW, RESB: 객체코드 없이 주소만 할당

## 3. 결론 및 보충할 점

### 3.1 성과
본 프로젝트를 통해 SIC 어셈블러의 기본 원리인 토큰 파싱, 심볼 테이블 관리, 주소 계산, 객체코드 생성 과정을 이해하고 구현했다. 특히 LOCCTR을 이용한 주소 계산과 심볼 테이블을 통한 라벨 참조 해석이 어셈블러의 핵심임을 확인했다. 과제에서 제시된 구조체와 함수 인터페이스를 활용하여 향후 SIC/XE 어셈블러로 확장 가능한 구조로 설계했다.

### 3.2 보충할 점
현재 구현에서는 다음 사항들이 보완되어야 한다.

첫째, 에러 처리가 미흡하다. 정의되지 않은 심볼을 참조하거나 잘못된 피연산자가 입력될 경우 0으로 처리하고 있어, 에러 메시지를 출력하고 어셈블리를 중단하는 로직이 필요하다.

둘째, SIC/XE의 확장 형식(Format 4)을 지원하지 않는다. '+' 접두사가 있는 명령어를 처리하고 4바이트 객체코드를 생성하는 기능이 추가되어야 한다.

셋째, nixbpe 비트를 정확히 계산하지 않고 있다. 현재는 단순히 OPCODE와 주소를 결합하고 있으나, 실제 SIC/XE에서는 n, i, x, b, p, e 플래그를 정확히 설정해야 한다.

넷째, 리터럴(=C'EOF', =X'05' 등)을 지원하지 않는다. 리터럴 테이블을 관리하고 LTORG 지시자나 프로그램 끝에서 리터럴 풀을 생성하는 기능이 필요하다.

다섯째, 표현식 평가(예: BUFFER+3, MAXLEN-RDREC)를 지원하지 않는다. 피연산자로 산술식이 오는 경우를 처리하는 파서가 필요하다.

여섯째, 재배치 가능한 프로그램을 위한 Modification Record를 생성하지 않는다. EXTREF, EXTDEF를 처리하고 외부 참조에 대한 재배치 정보를 기록하는 기능이 추가되어야 한다.

향후 프로젝트 #3에서는 이러한 기능들을 추가하여 완전한 SIC/XE 어셈블러를 구현할 계획이다.

## 4. 소스코드

첨부된 소스코드를 참조해주세요!

## 5. 깃허브 URL

https://github.com/사용자명/sysp2
(실제 깃허브 URL로 교체 필요)
